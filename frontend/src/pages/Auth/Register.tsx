/**
 * Register Page
 * (Placeholder - similar structure to Login)
 */

import styled from 'styled-components';

const RegisterContainer = styled.div`
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
`;

const Register: React.FC = () => {
  return <RegisterContainer><h1>Register Page (Implement similar to Login)</h1></RegisterContainer>;
};

export default Register;
